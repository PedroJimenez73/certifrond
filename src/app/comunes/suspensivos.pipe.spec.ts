import { SuspensivosPipe } from './suspensivos.pipe';

describe('SuspensivosPipe', () => {
  it('create an instance', () => {
    const pipe = new SuspensivosPipe();
    expect(pipe).toBeTruthy();
  });
});
